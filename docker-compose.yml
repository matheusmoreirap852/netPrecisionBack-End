services:
  task-manager-api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: netprecision-task-manager-api
    ports:
      - "${API_PORT:-8080}:8080"
    environment:
      SPRING_H2_CONSOLE_SETTINGS_WEB_ALLOW_OTHERS: "true"
      JAVA_OPTS: "-XX:MaxRAMPercentage=75.0"
    restart: unless-stopped
